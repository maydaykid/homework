using System;

namespace numbers
{
    class Program
    {
        static void Main(string[] args)
        {
             for(int secondNumber = 7; secondNumber <= 98; secondNumber += 7)
            {
                Console.WriteLine(secondNumber);
            }
            //выглядит лаконичнее, легче читается
        }
    }
}
